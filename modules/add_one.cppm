module;

#include <tyghbn/add_one.hpp>

/**
 * @brief Module interface for [add_one.hpp](@ref add_one.hpp).
 */
export module tyghbn.add_one;

export namespace tyghbn {
  // All names to be made public must be re-exported here explicitly.
  using tyghbn::add_one;
}
